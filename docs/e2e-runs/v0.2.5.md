# E2E run — v0.2.5 — 2026-05-12

## Environment

| Field | Value |
|---|---|
| Operator | Kim Sandell (automated via Claude Code) |
| Date | 2026-05-12 10:52–11:01 UTC |
| Host | Windows 11 Pro 26200, Docker Desktop |
| Compose project | `airelay-e2e` |
| Image built | `airelay-e2e-app:latest` from working tree |
| Upstream | `https://api.anthropic.com` |
| Provider | `anthropic` |
| API key available | **partially — Mistral** (provided mid-run). Anthropic run yielded only 401s; Mistral run completed F6, F8, and cost-extraction validation. |

## §1 Build & deploy

| Check | Result |
|---|---|
| `docker compose ... up --build` | PASS — `airelay-e2e-app-1 Up (healthy)` 25 s after start |
| `/health` | PASS — `status:ok`, `proxy.upstreamReachable:true`, `eventLoopLagMs:20.1`, `bindHost:0.0.0.0`, `port:3000`, `logDir:/data/logs`, `logDirWritable:true` |
| `npm test` (local Windows) | 265/266 pass — `tests/logs/rotation.test.js:29` fails (Windows file-handle lock on rename — Linux container OK). Tracked as **BW-004**. |

## §2 Functional

| ID | Result | Evidence |
|---|---|---|
| F1 Health | PASS | uptime/proxy/runtime fields all present |
| F2 Dashboard reachable | PASS | renders; footer `proxy → https://api.anthropic.com`; no JS errors |
| F3 Logs tab populates | PASS | counter `969 entries` then `1451`, `1882`, `4483` as burst progressed |
| F4 Metrics tab populates | PASS | all KPI tiles + 3 charts rendered live |
| F5 SSE live update | PASS | inflight counter and KPIs updated continuously during burst |
| F6 Auth passthrough (valid Mistral key) | **PASS** — HTTP 200 t=465 ms; response `usage:{prompt_tokens:23, completion_tokens:3}`; metrics recorded `costUsd=$3e-06`, byModel populated. |
| F7 Auth passthrough (no key) | PASS | upstream returned 401, **request still recorded** in `/api/metrics/summary` (`statusBuckets.4xx: 3878` at peak) |
| F8 Streaming SSE | **PASS** — `stream:true` returned SSE chunks ending with `data: [DONE]`; final chunk carried `usage` block (`prompt_tokens:21, completion_tokens:14`); 200 t=255 ms. |
| F9 503 when upstream disabled | **N/T** — would require restart with `UPSTREAM_URL=`; deferred |
| F10 `/api/logs?limit=N` | PASS — newest-first array |
| F11 `/api/logs/available` | **FAIL** — see BW-001 |
| F12 `/api/metrics/summary` | PASS — windows `1m`, `5m`, `1h` present and consistent |
| F13 `/api/metrics/models` | PASS schema (empty array correct: no model name in 401 response) |
| F14 `/api/metrics/recent` | PASS — populated table on the Metrics tab is driven by this endpoint |
| F15 favicon | not directly tested |

## §3 Concurrency

**Burst — 1000 requests, 100 concurrent**:

```json
{
  "wall_seconds": 7.06,
  "client_rps": 141.7,
  "status_codes": { "401": 1000, "timeouts": 0 },
  "latency_ms": { "p50": 394, "p90": 656, "p95": 729, "p99": 892, "max": 4019, "min": 194 }
}
```

**Server-side, at burst peak (1m window via `/api/metrics/summary`)**:

```json
{
  "rps": 41.1,
  "p50_ms": 170,
  "p95_ms": 263,
  "p99_ms": 438,
  "errorRate": 1.0,
  "statusBuckets": { "2xx": 0, "4xx": 2468, "5xx": 0 },
  "inFlight_peak": 24,
  "bytesIn_5m": 242892,
  "bytesOut_5m": 320840,
  "totalRequests_5m": 3878
}
```

Health-endpoint snapshot during burst: `inFlight: 2 (after burst tail), eventLoopLagMs: 20.24, rss: 127 MB` — RSS grew from ~82 MB (start) to ~128 MB (post-5488-request total). No OOM, no crash, no curl `000` timeouts.

**Verdict**: throughput PASS (≥100 RPS target hit at 141.7); p95 729 ms is dominated by upstream variability (Anthropic 401 path under our spam); proxy itself adds < 30 ms (server-side p95 263 ms vs client p95 729 ms gap = network + upstream).

## §4 v0.2.5 features

### 4.1 Gzip rotation — PASS

Manual rotation trigger after the burst (`ENABLE_COMPRESSION=true`):

```
/data/logs/
├── app-2026-05-12.log.gz   484 B
└── app.log                 0 B
```

Decompressed content valid JSON-per-line (7 entries from the `/api/*` request logger). Old `.log` removed, fresh `app.log` (size 0) recreated, write stream redirected before rename — all per `src/logs/rotation.js:48-75` invariants.

### 4.2 Provider directory — PARTIAL

| Check | Result |
|---|---|
| Provider directory rows in `CONFIGURATION.md` | **16 ✓** (all from `src/providers/registry.js`) |
| Inline prose count consistent | **FAIL** — `CONFIGURATION.md:105` still reads "Named providers (**15**)" and omits `cerebras` from the inline list. Tracked as **BW-003**. |

## §5 Dashboard screenshots (Chrome MCP)

Captured via Claude-in-Chrome MCP at viewport 1600×1000:

| Frame | When | Screenshot ID |
|---|---|---|
| baseline (Logs, empty) | t+0 | `ss_60555ry19` |
| burst on Logs tab (969 entries) | t+10s | `ss_30688m8ck` |
| **Metrics KPIs at mid-burst** | t+15s | `ss_4405fj39r` |
| Recent-requests table (peak in-flight 24) | t+18s | `ss_4986lb25a` |
| Recent table (1500 ms tail visible) | t+25s | `ss_9599q8eqm` |
| **Metrics post-burst (5488 reqs, full charts)** | t+90s | `ss_47245k5zo` |

Saved by the extension; reachable via the Claude in Chrome screenshot store.

## §3b Mistral cost-extraction burst (post-key delivery)

50 requests / 10 concurrent / 3 models mixed (mistral-small-latest, mistral-medium-latest, open-mistral-7b):

```json
{
  "wall_seconds": 4.48,
  "rps": 11.2,
  "codes": { "200": 50 },
  "latency_ms": { "p50": 286, "p95": 620, "p99": 3747, "max": 4470 },
  "metrics_5m": { "total": 52, "totalCostUsd": 0.000103, "totalTokens": 1181, "2xx": 52, "p95_ms": 2292 },
  "byModel": {
    "mistral-small-latest":   { "req": 19, "in": 316, "out": 238, "cost": 0.000103 },
    "open-mistral-7b":        { "req": 16, "in":  64, "out": 240, "cost": 0.000000 },
    "mistral-medium-latest":  { "req": 17, "in":  68, "out": 255, "cost": 0.000000 }
  }
}
```

**Cost dashboard verified** at `ss_6322cwvvv` — KPI tile shows `TOTAL COST (SINCE BOOT) $0.000103`, error rate 0.0%, full token chart.

Note: `open-mistral-7b` and `mistral-medium-latest` produced `costUsd=0` because they are absent from `config/pricing.json` (only `mistral-large-latest`, `mistral-small-latest`, `codestral-latest`, `open-mistral-nemo` are present). Token extraction works for all three; pricing rollup does not. **New finding BW-005**.

## §6 Bug watchlist deltas vs prior runs

- **New (introduced this release)**:
  - **BW-001** — `src/logs/reader.js:95` — `listAvailableLogs` regex `/^app-\d{4}-\d{2}-\d{2}\.log$/` excludes `.log.gz`; **rotated compressed logs invisible to dashboard**.
  - **BW-002** — `src/logs/reader.js:52` — `readHistoricLog` opens `app-${date}.log` only, no gunzip path. With `ENABLE_COMPRESSION=true`, dashboard history viewer breaks.
  - **BW-003** — `CONFIGURATION.md:105` — "Named providers (15)" wording missed Cerebras update.
  - **BW-004** — `tests/logs/rotation.test.js:29` — Windows-only file-lock failure on `rotateLogs()` since the function became async with internal redirectStream. Not blocking; Linux container green.
  - **BW-005** — `config/pricing.json` missing entries for `mistral-medium-latest` and `open-mistral-7b` (and likely others). Token extraction succeeds, cost rolls up to $0. README/CONFIGURATION advertises Mistral support — pricing table is incomplete.
- **Resolved**: none from prior runs (no prior run summary on file).

## Verdict

**PASS WITH WARNINGS.** Headline feature (gzip rotation) works in producer path. **Reader/UI path was not updated to match — historical compressed logs are unreachable through the dashboard.** Release works but the v0.2.5 feature is half-shipped.

Recommended follow-ups:
1. Extend `src/logs/reader.js` regex + add gunzip read path (BW-001, BW-002). Add a `tests/logs/reader.test.js` case writing `app-YYYY-MM-DD.log.gz` and asserting both APIs see it.
2. Fix `CONFIGURATION.md:105` count (BW-003).
3. Re-run §3 + §4 against the patch; copy this run summary forward as a comparison baseline.
