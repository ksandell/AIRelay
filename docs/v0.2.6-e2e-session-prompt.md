# v0.2.6 E2E verification — next-session prompt

Paste this into a fresh Claude Code session in the `AIRelay` repo to run the
full end-to-end regression suite against the merged v0.2.5 cleanup fixes
before tagging v0.2.6.

---

## STOP — ask for a fresh Mistral API key BEFORE doing anything else

The previous session leaked a Mistral key into `docs/v0.2.5-fix-session-prompt.md`
and it has been rotated. Do not run any E2E step that touches Mistral until
the user supplies a new key for this session.

**First message to the user:**
> "I need a fresh Mistral API key (set as `MISTRAL_API_KEY` env var in this
> shell — do not commit it to `.env`) before I can run §2 functional / §4
> gzip rotation against the live upstream. Paste the key here when you're
> ready and I'll continue."

Wait for the key. Then proceed.

---

## Mission

Run the full E2E regression suite at `docs/e2e-regression-suite.md` against
`develop` (now contains PR #109's merged cleanup), produce a run-summary
doc, and decide whether to tag v0.2.6.

## Pre-flight

- Branch: start on `develop` at commit `43fb1ff` ("fix: v0.2.5 cleanup —
  gzip reader path, Windows rotation, pricing, docs (#109)").
- `npm test` → 276/276 pass. `npm run lint` → clean. (If not, stop and fix.)
- `.env` must have `ENABLE_COMPRESSION=true` and `MISTRAL_API_KEY=<fresh key>`.
- Docker Desktop running.

## Plan

1. **§1 build & deploy** — `docker compose -f docker-compose.yml -p airelay-e2e up -d --build`. Wait for healthy.
2. **§2 functional matrix** — hit every endpoint, verify token + cost extraction. Use the fresh Mistral key. F11 (`/api/logs/available`) **must** now include `.log.gz` entries (this is the #104 verification). F-new (`/api/logs/history?date=...`) **must** decode gzipped historic logs (the #105 verification).
3. **§3 concurrency burst** — same harness as v0.2.5 run. Compare RPS / p95 / RSS against `docs/e2e-runs/v0.2.5.md` §3 baseline. Tolerances: RPS ±10%, p95 ±25%, RSS Δ ±30 MB.
4. **§4 gzip rotation** — trigger size-based rotation, confirm `.log.gz` appears, confirm round-trip via `/api/logs/history` API.
5. **§5 Windows-specific** — re-run `tests/logs/rotation.test.js` natively on Windows host (not just in container). Confirm the #107 fix held under the merged code.
6. **§6 watchlist** — confirm BW-001..005 from the v0.2.5 run are resolved. No new BW-NNN.
7. **Screenshots** — Chrome MCP at 1600×1000: baseline / burst / post-burst. Save to `docs/e2e-runs/v0.2.6-images/`.
8. **Run summary** — write `docs/e2e-runs/v0.2.6.md` using the template at §7 of the regression suite.
9. **CHANGELOG** — rename the `## [0.2.6] — Unreleased — v0.2.5 cleanup` heading to `## [0.2.6] — <YYYY-MM-DD> — v0.2.5 cleanup` only if the run is fully green and the user authorises tagging.
10. **Tag + release** — only on user confirmation: bump `package.json` to 0.2.6, `git tag v0.2.6`, push tag, `gh release create` per `docs/RELEASING.md`.

## Acceptance gate (must all be green before tagging)

- [ ] §2 functional matrix — every row PASS except known N/T rows
- [ ] §3 burst — within tolerances above
- [ ] §4 gzip rotation — `.log.gz` listed AND readable via API
- [ ] §5 Windows native — `npm test` 276/276 on Windows host
- [ ] §6 watchlist — BW-001..005 resolved, no new BW-NNN
- [ ] Run summary written
- [ ] User authorises tag

## Constraints

- Do not run E2E against Mistral without the fresh key.
- Do not commit the key to `.env` or any other file. Set in shell env only.
- Do not tag without explicit user authorisation.
- Follow `~/.claude/rules/common/release-docs.md` for the version bump.

## Reference

- Regression suite: `docs/e2e-regression-suite.md`
- Prior run baseline: `docs/e2e-runs/v0.2.5.md`
- Merged PR: https://github.com/ksandell/AIRelay/pull/109
- Closed issues: #104 #105 #106 #107 #108
