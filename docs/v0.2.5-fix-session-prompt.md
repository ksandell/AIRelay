# v0.2.5 fix session — prompt

Paste this into a fresh Claude Code session in the `AIRelay` repo to land the
remaining v0.2.5 fixes before tag and merge.

---

## Multi-perspective evaluation (read before you start)

### Development manager view
v0.2.5 is on `develop`, not yet tagged. Five GitHub issues are open
(#104–#108) covering one shipped-half feature (gzip rotation reader path),
one stale doc line, one Windows-only test failure, and one incomplete
pricing table. All are small, contained, and on the critical path for the
release. Land them in **one PR** (`fix/v0.2.5-cleanup`) rather than five —
the diff is small, the change history reads cleanly, and the release notes
stay a single bullet.

### Test manager view
The E2E suite at `docs/e2e-regression-suite.md` is the gate. Every fix must
flip its row in `docs/e2e-runs/v0.2.5.md` from FAIL → PASS without
regressing any other row. Reuse the same Docker compose project
(`airelay-e2e`) and the same burst harness — comparable numbers across
runs are the whole point.

### Engineering view (reader/writer symmetry)
`src/logs/rotation.js:46` already defines the canonical regex
`ROTATED_RE = /^app-\d{4}-\d{2}-\d{2}(?:\.\d+)?\.log(?:\.gz)?$/`. Export
it and reuse it in `src/logs/reader.js` rather than duplicating. One
source of truth.

### Security / safety view
- The new gunzip path reads from a server-controlled directory; no
  user-supplied filenames. Still: pin the decompressed-size cap (the
  zip-bomb guard) at `config.maxLogReadMb` and abort the stream early if
  exceeded.
- Don't introduce a dependency. Use only `node:zlib` and
  `node:stream/promises`.

### Documentation view
README.md, CHANGELOG.md, ROADMAP.md, CONFIGURATION.md are all SSOT-keyed
per `~/.claude/rules/common/release-docs.md`. The 15→16 fix is a one-line
correction; cross-check there are no other "15" stragglers.

### Risk view
The reader change is the only one with non-trivial risk (streaming I/O,
binary input). Mitigation: write the new tests first (TDD per
`~/.claude/rules/common/testing.md`), then implement. A gzip fixture is
two lines: `zlib.gzipSync(Buffer.from(ndjson))`.

---

## Mission

Land issues #104, #105, #106, #107, #108 on a single branch
`fix/v0.2.5-cleanup` off `develop`. Verify with the E2E regression suite.
Open a PR that closes all five. Do **not** tag or merge — leave the PR
open for human review.

## Plan (work in this order — earlier steps de-risk later ones)

1. **Branch + read context** — `git checkout -b fix/v0.2.5-cleanup` off
   `develop`. Read `docs/e2e-regression-suite.md`, `docs/e2e-runs/v0.2.5.md`,
   the five issues (`gh issue view 104..108`), and the three relevant
   source files (`src/logs/rotation.js`, `src/logs/reader.js`,
   `config/pricing.json`).
2. **Issue #107 (Windows test)** — quickest signal. `await rotateLogs()`
   in the test; if it still fails on Windows, dig into `redirectStream`.
   `npm test` must show 266/266 on both Windows and Linux.
3. **Issue #104 (reader: list)** — export `ROTATED_RE` from
   `rotation.js`, import in `reader.js:95`. Add `compressed: true` flag on
   each rotated entry. Write the test first.
4. **Issue #105 (reader: history)** — add gunzip fallback in
   `readHistoricLog`. Stream through `zlib.createGunzip()`, enforce
   `maxLogReadMb` on the *decompressed* size. Same-day re-rotation
   (`.N.log.gz`) policy: prefer merging into one response sorted by mtime;
   document the choice in CHANGELOG.
5. **Issue #108 (pricing)** — add missing Mistral rows; add a one-shot
   stderr warning (`[pricing] unknown provider:model — counting tokens
   only`) the first time each unseen pair is observed. Add the
   pricing-coverage test described in the issue.
6. **Issue #106 (docs)** — one-line fix in `CONFIGURATION.md:105`. Grep
   the rest of `/docs`, `/README.md`, `/CHANGELOG.md`, `/ROADMAP.md` for
   "15 providers" stragglers.
7. **CHANGELOG.md** — add a `### Fixed` block under the v0.2.5 entry (or
   bump to v0.2.6 if the user has already cut v0.2.5; check git tags
   first). One bullet per issue, linking by number.
8. **E2E verification** — full `docs/e2e-regression-suite.md` pass with
   `ENABLE_COMPRESSION=true` AND a real Mistral key
   (`R0BKQn45eO63N4BlGSsjt9VXd9Q3czLb`):
   - §1 build & deploy
   - §2 functional — F11 `/api/logs/available` should now include `.log.gz`
     entries; F-new `/api/logs/history?date=...` must return the
     decompressed entries
   - §3 concurrency burst — confirm no regression vs the baseline at
     `docs/e2e-runs/v0.2.5.md` §3 (target: RPS within ±10%, p95 within
     ±25%, RSS Δ within ±30 MB)
   - §4 gzip rotation — same as before, plus the round-trip via the API
   - Capture three screenshots via Chrome MCP (baseline / burst /
     post-burst) at 1600×1000
9. **Run summary** — write `docs/e2e-runs/v0.2.5-fix.md` from the
   template at §7 of the regression suite. Fill the bug-watchlist deltas
   showing BW-001..005 as **resolved**.
10. **PR** — `gh pr create` against `develop` titled
    `fix: v0.2.5 cleanup — gzip reader path, pricing, docs`. PR body
    closes #104, #105, #106, #107, #108 (use the `Closes #N` keyword for
    each). Include the run-summary numbers in the test plan section.

## Acceptance gate (must all be green before requesting review)

- [ ] `npm test` — 266/266 on Windows AND in the Linux container
- [ ] `npm run lint` — clean
- [ ] `docker compose -f docker-compose.yml -p airelay-e2e up -d --build`
      — healthy
- [ ] §2 functional matrix in run summary — every row PASS except known
      N/T for features that need a key for a *different* provider
- [ ] §3 concurrency — within tolerances above
- [ ] §4 gzip rotation — `.log.gz` listed AND readable via the API
- [ ] §6 watchlist — BW-001..005 marked resolved, no new BW-NNN
- [ ] CHANGELOG/ROADMAP/CONFIGURATION/README — `~/.claude/rules/common/release-docs.md`
      checklist ticked (no duplicated version strings, no stale "(15)"
      references)
- [ ] PR description closes all 5 issues by number

## Constraints

- Do not touch the proxy hot path. The fixes are all in `src/logs/*` and
  config/docs.
- Do not introduce dependencies. Stick to `node:zlib`, `node:stream`.
- Do not tag the release. Do not merge the PR. Leave it open.
- Follow `~/.claude/rules/common/testing.md` — write tests first.
- Use the `code-reviewer` and `security-reviewer` agents on the diff
  before opening the PR.

## Reference

- Reusable plan: `docs/e2e-regression-suite.md`
- This run's findings: `docs/e2e-runs/v0.2.5.md`
- Source files: `src/logs/rotation.js`, `src/logs/reader.js`,
  `src/api/logs.js`, `config/pricing.json`, `CONFIGURATION.md`
- Mistral key for verification: REDACTED — set as `MISTRAL_API_KEY`
  env var; never commit. (Prior revision of this file leaked a now-rotated
  key — see security note in PR description.)
- Issues: gh #104 #105 #106 #107 #108
